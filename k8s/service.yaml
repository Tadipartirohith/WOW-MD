apiVersion: v1
kind: Service
metadata:
  name: wow-backend
  namespace: wow
  labels: { app: wow-backend }
spec:
  type: ClusterIP
  selector: { app: wow-backend }
  ports:
    - name: http
      port: 80
      targetPort: 3000
